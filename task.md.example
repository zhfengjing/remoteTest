# 示例任务：添加用户登录功能

## 任务描述
为应用添加基础的用户登录功能，包括登录表单和身份验证逻辑。

## 具体要求

1. **创建登录组件**
   - 创建 `src/components/LoginForm.jsx` 文件
   - 包含用户名和密码输入框
   - 添加登录按钮
   - 使用 React Hooks 管理状态

2. **实现身份验证逻辑**
   - 创建 `src/services/auth.js` 文件
   - 实现 `login(username, password)` 函数
   - 使用 JWT token 存储认证信息
   - 添加基础的输入验证

3. **错误处理**
   - 处理登录失败情况
   - 显示友好的错误提示
   - 验证用户输入格式

4. **安全要求**
   - 不在代码中硬编码密码
   - 使用 HTTPS 传输认证信息
   - 实现基本的 CSRF 保护

## 技术栈
- React 18+
- Axios 用于 HTTP 请求
- JWT 用于身份验证

## 注意事项
- 遵循项目现有的代码风格
- 添加必要的注释
- 确保代码通过 ESLint 检查
- 编写单元测试（可选）

---

**提示**: 将此文件重命名为 `task.md` 并提交到非 main 分支，即可触发自动化流程。
